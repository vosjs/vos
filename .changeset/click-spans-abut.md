---
'@vosjs/studio-core': patch
'@vosjs/cli': patch
---

Two click spans the cluster rule split, less than a lead plus a hold apart, no longer overlap: the earlier span ends where the later one enters, and they chain into a pan. A push of such a take was refused for overlapping spans.
