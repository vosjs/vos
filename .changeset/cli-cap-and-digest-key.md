---
'@vosjs/cli': minor
---

`vos record` states the hosted recording cap from its own constant (`--max-duration` still overrides it); the plan table it used to read left `@vosjs/shared`. `vos digest` reports `tokensEstimate` (the old `tokensEstimateClaude` name stays in the JSON for one minor).
