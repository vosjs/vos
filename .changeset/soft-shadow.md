---
'@vosjs/studio-core': patch
---

The card's layered shadow gains a wide, faint ambient bloom (120 design px of blur, dropped well below the card) beside a lighter hairline and two grounding layers, so a large card on a light ground reads as lifted rather than outlined. `frame.shadow` stays the one strength the layers share.
