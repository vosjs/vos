---
'@vosjs/cli': minor
---

`still` in doc.json (the frame that stands for a take, output seconds), `vos plan --still <t>` and LAUNCH.md `still:`, `vos frames --at-still`; the kit's card stills read the same derivation as the shelf's cover; an end card keeps a freeze the take owns at its end.
