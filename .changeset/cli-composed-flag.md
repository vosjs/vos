---
'@vosjs/cli': patch
---

`vos deliver --composed` parses as a bare flag (it used to demand a value), `vos help` lists `vos actions` and says the recording cap is read live, and the README documents every verb and flag the binary accepts.
