---
'@vosjs/cli': minor
---

Many cards in the schema and the lints: a media overlay's `key` may be `media:<id>` (a document media, with its facts) and `frame` makes it a card (a browser bar, a lean, the layered shadow); `vos validate` names a stranger reference and a foreign card field; a push never treats a media reference as a take file.
