---
'@vosjs/studio-core': patch
---

The browser bar names the page under the playhead: another media's recorded page URL (its `meta.pageUrl`) is drawn in the address pill while that media plays, instead of the primary's.
