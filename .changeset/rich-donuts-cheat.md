---
'@vosjs/cli': patch
---

A type step takes `focus: false`, and a converted `press Enter` uses it, so the keystroke that submits a field no longer clicks it a second time and rings a click effect on empty space beside the text.
