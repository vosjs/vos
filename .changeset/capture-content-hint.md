---
'@vosjs/core': patch
---

`capture.encoder.contentHint` passes a WebCodecs content hint (`'text'` for screen content) to the capture-video encoder, so a software encoder can pick its screen-content tools.
